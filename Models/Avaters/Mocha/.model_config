export MODEL_NAME=モカ

export MODEL_DESCRIPTION="$(cat <<'EOS'
ロップイヤーなうさぎの女の子のアバターです
EOS
)"

export MODEL_QUEST_COMPATIBLE=対応

export DATA_CONTENTS="$(cat <<'EOS'
- Avater.unitypackage ファイル
- Mocha.blend 3D モデルデータ
- Mocha.fbx 3D モデルデータ
- BaseColor テクスチャデータ (png, psd※1, kra)
- Normal テクスチャデータ (png, psd※1, kra)
- LISENCE 利用規約
- README モデル概要

※1 psd ファイルは kra ファイル (Krita 専用画像形式 ) より生成しているため、 実際の画像とは異なる場合があります
EOS
)"

export GETTING_STARTED="$(cat <<'EOS'
簡易的な大まかなデータの利用方法です。
Unity での操作方法や VRChat SDK の利用方法についての詳細は詳しく説明している方がいますので、 Google 等で検索してみてください。

1. VRChat SDK3 Avater を導入済みの Unity プロジェクトを用意します。
2. (任意) 「ユニティちゃんトゥーンシェーダー※1」が必要であればプロジェクトにインポートします
3. `Avater.unitypackage` をダブルクリックし、プロジェクトにインポートします。
4. インポートされたディレクトリのシーンファイル `SampleScene.scene` を開きます。
5. VRChat SDK のツールを利用して公開します。

※1 [ユニティちゃんトゥーンシェーダー](https://unity-chan.com/download/releaseNote.php?id=UTS2_0)

Quest用にアップロードする場合は、耳のボーンを下に曲げて Dynamic Bone を外すことで行なえます
EOS
)"

export LAST_MODIFIED="$(cat <<'EOS'
- 2020/12/19 公開
EOS
)"